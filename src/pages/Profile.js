import * as React from 'react';
import JetCake from '../components';

export default function Profile(props){
    return (
        <JetCake.Layout>
            Profile
        </JetCake.Layout>
    )
}